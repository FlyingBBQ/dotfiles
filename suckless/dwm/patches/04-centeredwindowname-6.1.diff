--- tmpp/dwm.c	2019-08-21 22:40:43.557880083 +0200
+++ dwm.c	2019-08-21 22:51:45.221192974 +0200
@@ -696,7 +696,7 @@
 void
 drawbar(Monitor *m)
 {
-	int x, w, sw = 0;
+	int x, w, mid, sw = 0;
 	int boxs = drw->fonts->h / 9;
 	int boxw = drw->fonts->h / 6 + 2;
 	unsigned int i, occ = 0, urg = 0;
@@ -731,8 +731,9 @@
 
 	if ((w = m->ww - sw - x) > bh) {
 		if (m->sel) {
+            mid = (m->ww - TEXTW(m->sel->name)) / 2 - x - xbar;
 			drw_setscheme(drw, scheme[m == selmon ? SchemeSel : SchemeNorm]);
-			drw_text(drw, x, 0, w - (2 * xbar), bh, lrpad / 2, m->sel->name, 0);
+			drw_text(drw, x, 0, w - (2 * xbar), bh, mid, m->sel->name, 0);
 			if (m->sel->isfloating)
 				drw_rect(drw, x + boxs, boxs, boxw, boxw, m->sel->isfixed, 0);
 		} else {
