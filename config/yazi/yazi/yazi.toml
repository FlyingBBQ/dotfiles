[mgr]
linemode = "size"

[opener]
pdff = [
    { run = 'zathura "$@"', desc = "zathura", orphan = true, for = "unix" },
]

[open]
prepend_rules = [
    { mime = "application/pdf", use = [ "pdff", "reveal" ] },
]
